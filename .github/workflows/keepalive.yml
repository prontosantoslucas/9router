name: Keepalive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -sSfo /dev/null -w "%{http_code}" "${{ secrets.ZEABUR_URL }}/api/health" || echo "unreachable"
